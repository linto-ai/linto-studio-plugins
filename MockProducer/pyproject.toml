[project]
name = "mock-producer"
version = "0.1.0"
description = "MQTT mock producer for live transcription sessions"
requires-python = ">=3.12"
dependencies = [
    "aiomqtt>=2.0",
    "httpx>=0.27",
]
