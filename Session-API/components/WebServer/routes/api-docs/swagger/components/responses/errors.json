{
    "NotFound": {
        "description": "The requested resource does not exist.",
        "content": {
            "application/json": {
                "schema": { "$ref": "#/components/schemas/ErrorResponse" },
                "example": { "error": "Session abc-123 not found" }
            }
        }
    },
    "BadRequest": {
        "description": "The request is malformed or violates a business rule (e.g. invalid state transition, missing required parameter, refused without ?force=true).",
        "content": {
            "application/json": {
                "schema": { "$ref": "#/components/schemas/ErrorResponse" },
                "example": { "error": "Cannot pause session in status 'ready'. Only active sessions can be paused." }
            }
        }
    }
}
